node_modules
dist
.DS_Store
.env
.env.local

# Local credentials and tokens (OAuth client secrets, refresh tokens, etc.)
.secrets/

# Ignore the output video from Git but not videos you import into src/.
out
